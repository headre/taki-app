<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.example.myapplication">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme"
    android:usesCleartextTraffic="true">
    <activity
      android:name=".ui.login.LoginActivity"
      android:label="@string/title_activity_login">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
   <activity android:name=".FilmBookActivity" android:label="Order"/>
    <activity android:name=".FilmActivity" />
    <activity android:name=".FilmBookDetailActivity" android:label="Order"/>
    <activity android:name=".FilmUserActivity" android:label="User"/>
    <activity android:name=".PayActivity" android:label="Pay"/>
    <activity android:name=".ScreenActivity" android:label="Screen"/>
    <activity android:name=".FilmSearchActivity" android:label="Search"/>
    <activity android:name=".RegisterActivity" android:label="Register"/>
    <activity android:name=".CodeActivity" android:label="Order"/>
  </application>

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

</manifest>
